import React from 'react'

const Fetch = () => {
    const fetchData =()=>{
        
    }
  return (
    <>
     <button className="bg-lue-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded" onClick={() => handleDelete(index)} > Delete</button>
    </>
  )
}

export default Fetch